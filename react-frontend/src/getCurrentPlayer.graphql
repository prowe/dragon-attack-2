query GetCurrentPlayer($playerId: ID!) {
    player(id: $playerId) {
        __typename
        id
        name
        currentHealthPercent

        location {
            id
            name

            charactersPresent {
                ...AreaCharacter
            }
        }
    }
}