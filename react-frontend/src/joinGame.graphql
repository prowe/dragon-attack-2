mutation JoinGame($name: String!) {
    joinGame(name: $name) {
        id
        name

        location {
            id
            name

            charactersPresent {
                id
                name
                currentHealthPercent
            }
        }
    }
}